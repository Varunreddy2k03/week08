name: 05 - Deploy Monitoring

on:
  workflow_run:
    workflows:
      - "04 - Deploy to Production"
    types:
      - completed
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy-monitoring:
    name: Deploy Prometheus & Grafana
    runs-on: ubuntu-latest
    if: >
      ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    environment:
      name: production
    steps:
      - name: Login to Azure
        uses: azure/login@v3
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
      - name: Get AKS credentials
        run: |
          az aks get-credentials \
            --resource-group ${{ vars.AKS_RESOURCE_GROUP }} \
            --name ${{ vars.AKS_CLUSTER_NAME }} \
            --overwrite-existing
      - name: Set up Helm
        uses: azure/setup-helm@v4
        with:
          version: latest
      - name: Add Helm repos
        run: |
          helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
          helm repo add grafana https://grafana.github.io/helm-charts
          helm repo update
      - name: Create monitoring namespace
        run: |
          kubectl create namespace monitoring \
            --dry-run=client \
            -o yaml | kubectl apply -f -
      - name: Deploy Prometheus (lightweight, no operator/CRDs)
        run: |
          helm upgrade --install prometheus \
            prometheus-community/prometheus \
            --namespace monitoring \
            --set server.service.type=ClusterIP \
            --set alertmanager.enabled=false \
            --set prometheus-pushgateway.enabled=false \
            --set server.resources.requests.cpu=100m \
            --set server.resources.requests.memory=256Mi
      - name: Deploy Grafana
        run: |
          helm upgrade --install grafana \
            grafana/grafana \
            --namespace monitoring \
            --set service.type=ClusterIP \
            --set adminUser=admin \
            --set adminPassword=KoalaTech123! \
            --set resources.requests.cpu=100m \
            --set resources.requests.memory=128Mi
      - name: Wait for monitoring pods to be ready
        run: |
          kubectl -n monitoring rollout status deployment --timeout=300s || true
          kubectl -n monitoring get pods -o wide
      - name: Show monitoring resources
        run: |
          kubectl get pods -n monitoring
          kubectl get services -n monitoring